import { EnabledCompanyPipe } from './enabled-company.pipe';

describe('EnabledCompanyPipe', () => {
  it('create an instance', () => {
    const pipe = new EnabledCompanyPipe();
    expect(pipe).toBeTruthy();
  });
});
