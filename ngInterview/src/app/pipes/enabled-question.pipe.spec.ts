import { EnabledQuestionPipe } from './enabled-question.pipe';

describe('EnabledQuestionPipe', () => {
  it('create an instance', () => {
    const pipe = new EnabledQuestionPipe();
    expect(pipe).toBeTruthy();
  });
});
